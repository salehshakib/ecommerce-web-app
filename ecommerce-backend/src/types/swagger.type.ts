export interface SwaggerConfig {
  url: string;
  description: string;
}

export interface SwaggerServer {
  url: string;
  description: string;
}

export interface SwaggerTag {
  name: string;
  description: string;
}
